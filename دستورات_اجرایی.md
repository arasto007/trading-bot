# دستورات اجرایی ربات طلا (TradingBot)

ربات **تخصصی XAUUSD** — تایم‌فریم‌های **M5 / M15 / H4** — استراتژی Price Action (SMC).

---

## پنل اصلی (پیشنهادی)

روی این فایل **دوبار کلیک** کن:

👉 **`scripts/dashboard_server.py`** — یا `start\7_open_dashboard.bat`

| دکمه | کار |
|------|-----|
| بررسی آمادگی | چک MT5، نماد، پریست‌ها (`1_check_setup.bat`) |
| به‌روزرسانی مدل META | آموزش/به‌روزرسانی meta-labeler (`12_update_meta.bat`) |
| **روشن LIVE** | ربات واقعی + watchdog ری‌استارت خودکار (`3_live_loop_execute.bat`) |
| بک‌تست سفارشی | بازهٔ تاریخ/ساعت تهران از پنل (`8_backtest_custom.bat`) |
| وضعیت کامل | وضعیت پروسه و MT5 (`6_status.bat`) |
| **توقف ربات** | توقف امن + فلگ manual_stop (`5_stop_bot.bat`) |

> dry-run (بدون سفارش): `start\2_dry_run_once.bat`

---

## فایل‌های `start/` (بدون پنل)

| فایل | کار |
|------|-----|
| `1_check_setup.bat` | بررسی آمادگی MT5 و کانفیگ |
| `2_dry_run_once.bat` | یک چرخه بدون سفارش |
| **`3_live_loop_execute.bat`** | **LIVE + watchdog** (ری‌استارت ۵ دقیقه‌ای) |
| `4_live_daemon.bat` | همان watchdog در پس‌زمینه (بدون پنجره) |
| `5_stop_bot.bat` | توقف ربات |
| `6_status.bat` | وضعیت |
| `7_open_dashboard.bat` | باز کردن پنل HTA |
| `8_backtest_custom.bat` | بک‌تست بازهٔ انتخاب‌شده در پنل |
| `10_retrain_meta.bat` | آموزش کامل meta از صفر |
| `12_update_meta.bat` | به‌روزرسانی افزایشی meta |

---

## پیش‌نیاز

1. **MetaTrader 5** باز و لاگین
2. **Algo Trading** روشن
3. **XAUUSD_i** در Market Watch

---

## دستورات ترمینال

```powershell
cd "C:\Users\AMIR\Desktop\TradingBot new"
```

| کار | دستور |
|-----|--------|
| چک آمادگی | `python scripts/check_live_setup.py` |
| یک چرخه dry-run | `python -m tradingbot --live` |
| **LIVE + watchdog** | `python scripts/run_live_watchdog.py --execute` |
| توقف | `powershell -File scripts\stop_live_daemon.ps1` |
| وضعیت | `python scripts/status_live.py` |
| آمار meta | `python scripts/show_meta_stats.py` |

### بک‌تست

```powershell
python scripts/run_backtest.py --symbol XAUUSD --tf M15 --days 30 --risk 0.005
python scripts/run_backtest.py --symbol XAUUSD --tf M5 --days 30 --risk 0.005
python scripts/run_backtest.py --symbol XAUUSD --tf H4 --days 90 --risk 0.005
python scripts/backtest_custom_range.py --start "2026-06-01 08:00" --end "2026-06-07 20:00" --balance 200
```

---

## تنظیمات کلیدی

| مورد | مقدار |
|------|--------|
| نماد | XAUUSD (`XAUUSD_i` در بروکر) |
| TF (چرخه live پیش‌فرض) | **فقط M5 (`5m`)** — `get_live_config()` وقتی router/adaptive/vol روشن است `TIMEFRAMES=["5m"]` می‌کند *(به‌روزرسانی شده — وضعیت واقعی کد در تاریخ 2026-09-10)* |
| پریست‌های per-TF (بک‌تست / bias / تاریخچه) | M5 / M15 / H4 در `pa_symbol_tf_presets.py` — **نه** اجرای همزمان هر سه TF در هر تیک live |
| ریسک | 0.5% هر معامله |
| max پوزیشن | ۳ کل / ۲ per symbol |
| M5 (پریست live پیش‌فرض) | NY sweep 12–15 UTC؛ meta از env/preset |
| M15 (پریست؛ نه چرخه همزمان live) | سشن 8–20 UTC، ADX، HTF H4، partial TP؛ meta در preset |
| H4 (پریست / bias؛ نه چرخه همزمان live) | 24h، HTF D1 |

فایل‌های کانفیگ:
- `tradingbot/config/live.py`
- `tradingbot/config/pa_symbol_tf_presets.py`

> نکته: `TIMEFRAME_CONFIGS` و `get_timeframe_config()` در `live.py` تعریف شده‌اند ولی در مسیر
> live/backtest **مصرف‌کننده ندارند** (dead config). آستانه‌های per-TF «ULTRA AGGRESSIVE» آن‌ها
> روی رفتار فعلی اعمال نمی‌شوند. *(به‌روزرسانی شده — 2026-09-10)*

---

## Meta-Labeler

مدل ML **جهت قیمت پیش‌بینی نمی‌کند** — فقط کیفیت سیگنال را فیلتر می‌کند.

| TF | وضعیت | توضیح |
|----|--------|--------|
| M15 | فعال | OOS معتبر — threshold از آموزش |
| M5 / H4 | خاموش | داده یا OOS کافی نبود |

لاگ تصمیم‌ها: `data/meta_decisions.jsonl`  
وضعیت مدل: `models/meta_labeler_info.json`

---

## Watchdog

`run_live_watchdog.py` ربات را بعد از کرش یا قطع MT5 **۵ دقیقه بعد** دوباره بالا می‌آورد.  
توقف دستی: دکمه «توقف ربات» در پنل (فلگ `data/manual_stop.flag`) — نه بستن پنجره با X.
