# Phase 78 — Time / Reversal Exit Forensics

**TIME_DEPENDENCY (exit signature):** `SECONDARY`
Losers with MFE>=0.5R: `157`; MFE>=1R: `94`.

Buckets are predeclared. No arbitrary minute search. Entry TOD is production-constrained.
