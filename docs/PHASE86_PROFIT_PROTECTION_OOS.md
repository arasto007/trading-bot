# Phase 86 — Profit-Protection OOS Consistency

OOS reported, not used to select. Top-1/top-5 removals are diagnostics. Baseline unchanged.
Survivors (HELPFUL + tail not DESTROYED + TRAIN and VAL POS): `[]`

- `PEAK_RETRACE_EXIT` survives=`False` TRAIN=`POS` VAL=`NEG` OOS=`NEG` RECENT=`POS` without_top1=`True` without_top5=`True`
- `MFE_FRACTION_FLOOR` survives=`False` TRAIN=`POS` VAL=`NEG` OOS=`NEG` RECENT=`POS` without_top1=`True` without_top5=`True`
- `SWING_PROTECTION` survives=`False` TRAIN=`POS` VAL=`NEG` OOS=`NEG` RECENT=`NEG` without_top1=`False` without_top5=`False`
- `ATR_NORMALIZED_RETRACE` survives=`False` TRAIN=`None` VAL=`None` OOS=`None` RECENT=`None` without_top1=`None` without_top5=`None`
