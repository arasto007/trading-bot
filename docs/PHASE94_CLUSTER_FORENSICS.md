# Phase 94 — Signal / Event Cluster Forensics

RESEARCH ONLY. Signals are not independent.

jsonl has 2847 rows vs 419 resolved (utc_date, side) events. Protection research must use the event tape. Per-signal protection would double-count the same liquidity-sweep move.

median signals/event=`6.0` winners=`6.0` losers=`5.0` realized>10=`3.0`
singleton events=`40` clustered=`379`

Future protection research MUST use the event tape, not raw signals.
