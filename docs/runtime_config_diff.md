# Runtime Config Diff — Declared vs Actually Used

**Phase:** 34A  
**Live path:** ADAPTIVE_REGIME + `USE_ML_KERNEL=false`

This table compares what `.env.example` **declares** vs what the **active live path actually reads**.

Legend:
- **ACTIVE** — affects live trading today
- **INACTIVE** — present in old `.env.example` but ignored on adaptive path
- **SHADOWED** — defined but overridden by higher-priority setting

---

## Engine & Mode

| Variable | Declared in `.env.example` | Actually used? | Read from | Notes |
|----------|---------------------------|----------------|-----------|-------|
| `USE_ML_KERNEL` | `false` | **ACTIVE** | `ml/integration/config.py` | Must be explicitly set |
| `ADAPTIVE_REGIME_ENABLED` | (not in example; default true in live.py) | **ACTIVE** | `live.py`, `factory.py` | Selects adaptive engine |
| `ADAPTIVE_CONFLUENCE_ONLY` | (not in example; default true) | **ACTIVE** | `adaptive_regime.py` | Limits sub-strategies |
| `VOL_REGIME_ENABLED` | (hardcoded true in live.py) | **SHADOWED** | `factory.py` | Adaptive wins first |
| `VOL_REGIME_SKIP_TQ` | (not in example) | **INACTIVE** for adaptive | vol registry only | Adaptive sets tq_skipped in metadata |
| `ALLOW_LEGACY_FALLBACK` | (not in example) | **INACTIVE** | ML config only | ML off |

---

## Risk & Limits

| Variable | Declared | Actually used? | Read from | Notes |
|----------|----------|----------------|-----------|-------|
| `RISK_PER_TRADE` | via live.py 0.005 | **ACTIVE** | `risk_gate.py` | Lot sizing |
| `VOL_REGIME_COOLDOWN_BARS` | via live.py 12 | **ACTIVE** | `risk_gate.py`, tracker | ~60 min on M5 |
| `VOL_REGIME_MAX_TRADES_PER_DAY` | via live.py 3 | **ACTIVE** | `live_risk_tracker.py` | |
| `VOL_REGIME_MAX_CONCURRENT` | via live.py 1 | **ACTIVE** | `risk_gate.py` | |
| `MAX_DAILY_RISK` | via live.py 0.04 | **ACTIVE** | kill switch, risk_logic | 4% |
| `VOL_REGIME_RISK_PER_TRADE_PCT` | via live.py 0.5 | **METADATA ONLY** | adaptive registry | **Not wired to lot size** |

---

## Filters (Declared but INACTIVE on adaptive)

| Variable | Declared in `.env.example` | Actually used? | Why inactive |
|----------|---------------------------|----------------|--------------|
| `ENABLE_RSI_FILTER` | `true` | **INACTIVE** | ML `phase19c/filters.py` only |
| `ENABLE_ADX_FILTER` | `true` | **INACTIVE** | ML only |
| `RSI_MIN/MAX` | 40/60 | **INACTIVE** | ML only |
| `ADX_MIN/MAX` | 15/50 | **INACTIVE** | ML only |
| `PHASE22C_*` | various | **INACTIVE** | ML kernel stack only |
| `TREND_MODEL_VERSION` | v41 | **INACTIVE** | ML phase17d only |
| `TRADINGBOT_SIGNAL_FILTER` | unset → OFF | **INACTIVE** (default) | WPSQF off unless set |
| `TRADINGBOT_WPSQF_THRESHOLD` | 77.56 default | **INACTIVE** when WPSQF off | |

---

## Skipped by code for ADAPTIVE_REGIME signals

These gates exist in codebase but **early-return skipped** in `risk_gate.py`:

| Gate | Skipped? | Evidence |
|------|----------|----------|
| Meta-labeler | Yes | L174 `_is_vol_regime_signal` |
| HTF alignment | Yes | L277–278 early return |
| PA market filters | Yes | L295+ never reached |
| Trade Quality engine | Yes | adaptive metadata `tq_skipped: True` |

---

## Hardcoded (not env) but ACTIVE

| Setting | Value | Source |
|---------|-------|--------|
| Session window | 12–17 UTC | `adaptive_regime.py` SESSION_HOURS |
| Symbol / TF | XAUUSD M5 only | `get_live_config()`, registry |
| Rule confidence | 0.60 fixed | `VOL_REGIME_RULE_CONFIDENCE` |
| EOD close | 23:55 | `live.py` EOD_CLOSE_ENABLED |
| Friday close | 20:00 | PRICE_ACTION preset |
| Trailing / partial TP | On | `Mt5PositionManager` |
| Loop interval | 30s | `live.py` LOOP_INTERVAL |

---

## Recommended `.env` for live truth

Use **`config/live_runtime.env.example`** instead of the full `.env.example` when running adaptive live.

Verify at startup: `logs/runtime_truth.json` (auto-generated by LiveRunner and `tools/runtime_truth_report.py`).

---

## Summary

| Category | Count |
|----------|-------|
| ACTIVE env vars | ~8 (+ MT5 creds) |
| INACTIVE vars in old `.env.example` | ~15+ |
| SHADOWED | 1 (`VOL_REGIME_ENABLED`) |
| Hardcoded live behavior | Session, symbol, confidence |

**Truth source of record:** `logs/runtime_truth.json`
